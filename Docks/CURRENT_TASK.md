# CURRENT_TASK.md — تمرکز فعلی سیستم

## ۱. درخت تمرکز (Focus Tree)
```
src/
└── services/
    └── temporal/
        ├── temporalCsvService.ts               [کامل شده ✅]
        └── temporalService.ts                  [کامل شده ✅]
```

## ۲. جزئیات تکمیلی تسک فعلی (پیاده‌سازی سهمیه‌بندی هوشمند رویدادها)

### عنوان و هدف
**پیاده‌سازی مدیریت سهمیه (Quota) برای لینک‌های فصلی افزایشی در `temporalService.ts` همراه با پشتیبانی از تشخیص خودکار فصلی بودن صفحه مبدأ**
به‌روزرسانی تابع `applyTemporalBoost` برای پذیرش `sourcePageTitle` به عنوان مشخصه کانتکست جهت تشخیص زماندار (Seasonal) بودن صفحه مبدأ. در صورت انطباق با کلمات کلیدی، سهمیه لینک‌های فصلی روی عدد ۵ و در غیر این‌صورت روی عدد ۳ تنظیم شده و به ترتیب بر اساس بیشترین مقدار امتیاز جدید اعمال می‌شود؛ سایر کاندیداهای مازاد با حفظ رتبه طبیعی به حالت خنثی بازگردانده می‌شوند.

## ۳. وضعیت تسک‌های بعدی

- **تسک F2.5 — UI: بخش تنظیمات Live در Config.tsx** [کامل شده ✅]
- **تسک F2.6 — UI: Quick Toggle + اعمال Boost در PageDetail.tsx** [کامل شده ✅]
- **تسک اصلاح لایه CSV — انطباق با هدرها و جداکننده‌های جدید** [کامل شده ✅]
- **تسک ارتقای هوشِ فصلی — منطق سهمیه‌بندی (Quota)** [کامل شده ✅]

---

## ۴. رله کانتکست (Context Relay)

### وضعیت پروژه و کارهای قبلی:
- الگوریتم سهمیه‌بندی به خوبی پیاده‌سازی و یکپارچه شد، ضریب‌های افزایشی مازاد به طور بهینه به ضریب خنثی بازآرایی یا ریست شدند.
- کدهای مربوطه در تمام فراخوانی‌ها اعم از `PageDetail.tsx` و `analysisService.ts` با فیلد `sourcePageTitle` هماهنگ گردیدند.
- فرآیند بررسی و تایید نهایی کامپایل با موفقیت کامل انجام شد.
