# CURRENT_TASK.md — وضعیت فعلی سیستم و تسک جاری

## ۱. درخت تمرکز (Focus Tree)
فایل‌هایی که در این سشن اجرایی ارتقا یافتند و نهایی شدند:
```text
CONTEXT_FILES: [
  "src/constants/categories.ts",
  "src/utils/scorer.ts",
  "src/pages/Config.tsx",
  "src/utils/queueProcessor.ts",
  "src/components/CandidateCard.tsx",
  "src/pages/PageDetail.tsx",
  "server.ts",
  "src/utils/gemini.ts"
]
```

---

## ۲. وضعیت نهایی تسک‌ها:

### ✅ تسک ۱ — اضافه کردن ثابت‌های زمانی و فصلی (انجام شد)
* **دستاورد:** ثابت‌های زمانی پایه‌ای شمسی و فصل‌ها همراه با بونوس‌های مبدا (۱۰) و مقصد (۵) به `categories.ts` افزوده شد.

### ✅ تسک ۲ — بازنویسی کامل موتور امتیازدهی با منطق چندلایه (انجام شد)
* **دستاورد:** موتور امتیازدهی هوشمند بر اساس فیلتر سخت زمانی/فصلی، امتیاز پایه و افزدن بونوس‌ها به صورت کاملاً چندلایه بازنویسی شد.

### ✅ تسک ۳ — پیاده‌سازی Reactivity تنظیمات در Config (انجام شد)
* **دستاورد:** تغییر ضرایب در صفحه تنطیمات فوراً کاندیداها و صف پردازش قبلی را برای باز‌محاسبه هوشمند پاک‌سازی می‌کند.

### ✅ تسک ۴ — بهبود پرامپت AI با درک نیت کاربر (انجام شد)
* **دستاورد:** پرامپت ارسالی به Gemini با اضافه شدن بونوس‌ها، تگ‌های مرتبط و فیلتر زمانی بهینه‌سازی شد. همچنین برای جلوگیری از خطای طول کانتکست، لیست کاندیداها به ۳۰ مورد برتر محدود گردید.

### ✅ تسک ۵ — آپدیت کامپوننت CandidateCard برای نمایش بونوس‌ها (انجام شد)
* **دستاورد:** نمایش بصری بونوس‌های مبدا (با آیکون خانه) و بونوس‌های مقصد (با آیکون هدف) در کارت‌های مرتبط کاندیدا در صفحه جزییات پیاده‌سازی شد.

### ✅ تسک ۶ — تست و اطمینان از یکپارچگی (انجام شد)
* **دستاورد:** تست خودکار در `src/utils/scorer.test.ts` با موفقیت پاس شد.

---

## ۳. رفع خطاهای API و PayloadTooLargeError:
* **دستاورد:** لایه محدودیت حجم ورودی Middleware بدنه اکسپرس در فایل `server.ts` جهت مدیریت آسان کانتکست‌های طولانی و بزرگ به مگابایت بالا (`50mb`) ارتقا پیدا کرد و خطای PayloadTooLargeError به صورت ریشه‌ای برطرف گردید.

---

## ۴. وضعیت کل پروژه
* پروژه با موفقیت کامپایل و تست شد و کلیه چرخه‌های پردازش تحلیل هوشمند تکی و صف انبوه با پایداری کامل در حال اجرا هستند.
