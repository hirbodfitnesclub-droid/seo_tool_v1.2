# CURRENT_TASK.md — وضعیت فعلی

## تسک جاری: تسک ۱۰ — تست نهایی و رفع باگ

### وضعیت: جاری (مهاجرت به Full-stack و رفع مشکل Gemini API انجام شد)

---

## ۱. درخت تمرکز (Focus Tree)
```
CONTEXT_FILES: ["Docks/ARCHITECTURE.md", "server.ts", "src/pages/ProjectPages.tsx", "src/pages/PageDetail.tsx", "src/utils/gemini.ts"]
```

---

## ۲. جزئیات کامل تسک فعلی

### هدف
تست کامل سیستم جدید و اطمینان از عملکرد صحیح تمام قسمت‌ها. رفع باگ‌های پیدا شده حین اجرا.

### اقدامات انجام شده
- [x] به‌روزرسانی اسکیمای دیتابیس
- [x] آپدیت موتور امتیازدهی (matched_tags)
- [x] ساخت تابع ذخیره کاندیداها + Route جدید
- [x] صفحه لیست صفحات پروژه (ProjectPages)
- [x] صفحه جزئیات صفحه (PageDetail)
- [x] پردازش AI تکی (یک صفحه)
- [x] سیستم صف پردازش AI (تحلیل کلی)
- [x] اتصال UI به سیستم صف
- [x] آپدیت صفحات Home و Results
- [x] رفع باگ "Cannot read properties of undefined (reading find)"
- [x] مهاجرت به معماری Full-stack برای امنیت و استفاده از کلید سیستمی Gemini
- [x] پیاده‌سازی Proxy API برای Gemini (نسخه مدرن SDK)
- [x] پنل تنظیمات پیشرفته تحلیل (انتخاب مدل و حالت پردازش)
- [x] به‌روزرسانی مدل Gemini به نسخه‌های مدرن (3.1 و 3)
- [x] نوسازی رابط کاربری (Next Gen SEO UI)

---

## ۳. وضعیت تسک‌ها

| # | تسک | وضعیت |
|---|---|---|
| ۱ | به‌روزرسانی اسکیمای دیتابیس | ✅ تمام |
| ۲ | آپدیت موتور امتیازدهی (matched_tags) | ✅ تمام |
| ۳ | ساخت تابع ذخیره کاندیداها + Route جدید | ✅ تمام |
| ۴ | صفحه لیست صفحات پروژه (ProjectPages) | ✅ تمام |
| ۵ | صفحه جزئیات صفحه (PageDetail) | ✅ تمام |
| ۶ | پردازش AI تکی (یک صفحه) | ✅ تمام |
| ۷ | سیستم صف پردازش AI (تحلیل کلی) | ✅ تمام |
| ۸ | اتصال UI به سیستم صف | ✅ تمام |
| ۹ | آپدیت صفحات Home و Results | ✅ تمام |
| ۱۰ | تست نهایی و رفع باگ | 🔄 جاری |

---

## ۴. رله کانتکست (Context Relay)

### تغییرات اساسی سیستم جدید:
سیستم قبلی همه صفحات را یکجا به AI می‌فرستاد. سیستم جدید:

1. **مرحله اول (بدون AI):** امتیازدهی الگوریتمی برای پیدا کردن ۲۰ کاندیدای هر صفحه بر اساس تگ‌های مشترک
2. **مرحله دوم (با AI):** بررسی دانه‌به‌دانه — هر صفحه جداگانه به AI فرستاده می‌شود
3. **ذخیره فوری:** بعد از هر پاسخ AI، نتیجه همان لحظه ذخیره می‌شود
4. **قابلیت resume:** اگر وسط کار قطع شد، از همان جا ادامه می‌دهد
5. **صفحه جزئیات:** هر صفحه صفحه مخصوص خودش را دارد با قابلیت ویرایش دستی

### فایل‌های جدیدی که ساخته می‌شوند:
- `src/pages/ProjectPages.tsx` — لیست صفحات + دکمه تحلیل کلی
- `src/pages/PageDetail.tsx` — جزئیات صفحه + ویرایش دستی
- `src/utils/candidateStorage.ts` — ذخیره کاندیداها
- `src/utils/queueProcessor.ts` — پردازش صف AI
- `src/hooks/useAnalysisQueue.ts` — مدیریت صف
- `src/components/QueueProgress.tsx` — نمایش پیشرفت

### فایل‌هایی که ویرایش می‌شوند:
- `src/db.ts` — اسکیمای جدید
- `src/utils/scorer.ts` — اضافه کردن matched_tags
- `src/utils/gemini.ts` — تابع جدید برای یک صفحه
- `src/App.tsx` — route‌های جدید
- `src/pages/Home.tsx` — لینک‌های جدید
- `src/pages/Results.tsx` — حذف منطق analyze
- `src/pages/Config.tsx` — تغییر مسیر
