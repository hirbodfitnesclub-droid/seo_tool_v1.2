import { CandidateWithTags } from './scorer';

export function buildSinglePagePrompt(
  sourcePage: { title: string; categories: string | object },
  candidates: CandidateWithTags[]
): string {
  let sourceCategoriesObj: any = {};
  if (typeof sourcePage.categories === 'string') {
    try {
      sourceCategoriesObj = JSON.parse(sourcePage.categories);
    } catch {
      sourceCategoriesObj = {};
    }
  } else {
    sourceCategoriesObj = sourcePage.categories || {};
  }

  // انتخاب حداکثر ۳۰ کاندیدای برتر برای پرامپت جهت مدیریت بهینه کانتکست و تمرکز مدلی
  const topCandidates = candidates.slice(0, 30);

  return `
تو یک متخصص SEO و معمار ارشد لینک‌سازی داخلی برای سایت تور مسافرتی بزرگ «نهال‌گشت» هستی.

وظیفه تو تحلیل ویژگی‌ها و تگ‌های صفحه فعلی (Source Page) و انتخاب دقیقاً ۱۵ لینک داخلی عالی و منطبق از بین ۳۰ کاندیدای پیشنهادی (Smart Pool) بر اساس معیارهای سئویی و بیزینسی مشخص در زیر است.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 مهم‌ترین وظیفه تو: درک نیت کاربر (User Intent Decoding)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
قبل از انتخاب نهایی لینک‌ها، باید نیت کاربر را در قالب فیلد "user_intent" مشخص کنی:
۱. **چه کسی** به این صفحه می‌آید؟ (مثلاً کاربری هوشمند که از اهواز سفر می‌کند)
۲. **چه زمانی** می‌خواهد سفر کند؟ (توجه به تگ «ماه_تقویمی_برگزاری» و «فصل_برگزاری»)
۳. **دغدغه اصلی** کاربر چیست؟ (مقصد مشترک، ارزان‌قیمت بودن یا اقامت‌گاه‌های تفریحی و لوکس)
۴. **سفر کاربر در سایت چگونه تکمیل می‌شود؟** ارائه لینک به صفحات جایگزین یا مکمل به جای کپی تکراری.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 ماتریس قوانین الگوریتم و اولویت‌بندی لینک‌ها (بسیار مهم!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
لینک‌های انتخابی و دلایل تو باید عمیقاً قوانین زیر را منعکس کنند:

۱. **قانون حاکم (The Pillar Rule - قانون ۱۱):**
   صفحاتی که عمومی و اصلیِ مقصد هستند و فاقد مبدا، فاقد ماه/فصل برگزاری خاص و فاقد هتل مشخصی در عنوان می‌باشند (به عنوان صفحات دسته ۱ یا صفحات پیلارِ مقصد شناخته می‌شوند) بیشترین اعتبار سئویی را دارند. این صفحات در کاندیداها با بونوس بیش از ۱۰۰۰ مشخص شده‌اند و باید همواره در بالای پیشنهادات تو قرار بگیرند تا تضمین شود صفحات پیلار رتبه اول یا دوم توصیه‌ها را دارند.

۲. **قلسل فدا کردن (Sacrifice Hierarchy - قانون ۹):**
   سلسله‌مراتب اهمیت ویژگی‌ها به نفع هم‌گرایی دسته بالا در سیستم به این صورت است: 
   Destination > Time > Origin > Hotel
   یعنی در صورت نداشتن گزینه ایده‌آل، سیستم به تدریج جزئیات هتل، سپس شهر مبدا، سپس تگ‌های زمان و در نهایت مقصد را قربانی می‌کند. اولویت بیشترین به مقصد و زمان داده می‌شود.

۳. **قانون اولویت‌دهی مبدأ (Origin Retention - قانون ۴):**
   اگر کاربر در حال مشاهده صفحه‌ای با مبدأ خاصی است (مثال: تور کیش از مشهد)، نگهداشتن او در تورهای مناسب از مبدأ مشهد بالاترین اولویت را دارد.

۴. **قانون توسعه جغرافیایی (Geo Expansion - قانون ۶):**
   اگر مقصد کاندیدا متغیر باشد اما هر دو در یک کشور (مثل آنتالیا و کوش‌آداسی در ترکیه) و در یک ماه/فصل مشابه برگزار شوند، اولویت تشویقی بگیرید (دارای بونوس جغرافیایی +۴۵ امتیاز).

۵. **قوانین تقابل ستاره هتل و بودجه (Hotel Star & Budget Co-activity - قانون ۷):**
   - اگر عنوان صفحه منبع شامل "ارزان" یا تگ‌های کلاسی اقتصادی است:
     * اولویت اول کاندیداهای هتل‌های ۳ ستاره یا ارزان (+۲۰ امتیاز بونوس).
     * کاندیداهای هتل ۵ ستاره و لوکس به شدت تضعیف شوند (-۵۰ امتیاز جریمه سنگین).
   - اگر عنوان صفحه منبع شامل "لوکس" یا تگ‌های کلاسی لاکچری است:
     * اولویت اول کاندیداهای هتل‌های ۵ ستاره (+۲۰ امتیاز بونوس).
     * کاندیداهای هتل ۳ ستاره و اقتصادی به شدت تضعیف شوند (-۵۰ امتیاز جریمه سنگین).

۶. **قانون جایگزینی هتل (Hotel Fallback - قانون ۸):**
   اگر صفحه فعلی درباره هتل خاصی است (مثال: هتل ترنج) و کاندیدای دقیق با همان هتل وجود ندارد:
   - اولویت با صفحاتی است که نام هتل در آن‌ها کلی‌تر/ستاره‌ای است (مثلاً تور کیش هتل‌های ۵ ستاره) با بونوس +۱۵ امتیاز.
   - اولویت ثانویه با کاندیدایی است که نام هتل دیگری در همان مقصد را یدک می‌کشد با بونوس +۵ امتیاز.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 صفحه فعلی (Source Page)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
عنوان: ${sourcePage.title}
اطلاعات طبقه‌بندی و تگ‌ها: ${JSON.stringify(sourceCategoriesObj, null, 2)}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 ۳۰ کاندیدای پیشنهادی (پیش‌فیلتر شده و اولویت‌دار بر اساس الگوریتم ۵ مرحله‌ای پیشرفته)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تحلیل تو باید روی گزینه‌های زیر متمرکز باشد. با استفاده از تگ‌های کامل زیر، بهترین گزینه‌ها را برای سئو انتخاب کن:
${topCandidates.map((c, i) => {
  let tagsStr = 'فاقد تگ مشخص';
  if (c.categories) {
    tagsStr = typeof c.categories === 'string' ? c.categories : JSON.stringify(c.categories, null, 2);
  }
  return `
${i + 1}. [شناسه محصول ID: ${c.page_id}] ${c.title}
   📊 امتیاز فنی الگوریتمی: ${c.score}
   🏷️ تگ‌های کامل کاندیدا: ${tagsStr}
   🏠 بونوس مبدا یکسان: ${c.origin_bonus > 0 ? `✅ ${c.origin_bonus}` : '—'}
   🎯 بونوس مقصد یکسان: ${c.destination_bonus > 0 ? `✅ ${c.destination_bonus}` : '—'}
   🔹 تگ‌های انطباق مشترک: ${c.matched_tags ? c.matched_tags.join(' | ') : ''}
`;
}).join('\n')}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📤 فرمت خروجی (حتمی و تخطی‌ناپذیر!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
از ۳۰ کاندیدای بالا، **دقیقاً ۱۵ مورد** از مرتبط‌ترین و بهترین صفحات را بر اساس امتیاز و تطابق تگ‌های سئویی بالا برگزین.
اگر تعداد کل کاندیداهای ورودی کمتر از ۱۵ مورد باشد، تمام موارد موجود را بازگردان، اما در غیر این صورت خروجی نهایی تو در لیست "selected_links" باید شامل **دقیقاً ۱۵ مورد** باشد (نه کمتر و نه بیشتر). برای هر مورد توصیه‌شده، دلیل سئویی دقیق متمایز و متقاعدکننده بنویس.

خروجی را **فقط** به صورت JSON خالص برگردان (بدون علامت‌های \`\`\`json و \`\`\`، بدون هیچ کلام فارسی در قبل یا بعد از آبجکت JSON):

{
  "user_intent": "تحلیل فشرده نیت کاربر بر اساس ۴ پرسش اساسی فوق",
  "selected_links": [
    { 
      "page_id": 12, 
      "title": "عنوان دقیق کاندیدا از لیست بالا", 
      "reason": "دلیل منطقی، کارشناسی، ترغیب‌کننده و هماهنگ با ماتریس سئو (مثلاً انطباق مبدأ اصفهان و همگرایی لوکس بودن هتل ۵ ستاره)"
    }
  ]
}
`;
}

export function buildPrompt(pages: any[], candidatesMap: Map<number, any[]>, maxLinks: number): string {
  let prompt = `
تو یک متخصص SEO حرفه‌ای هستی. وظیفه تو انتخاب بهترین لینک‌های داخلی برای صفحات یک سایت گردشگری (نهال‌گشت) است.

دستورالعمل:
1. برای هر صفحه منبع (source_page)، از بین کاندیداهای پیشنهادی، بهترین لینک‌ها را انتخاب کن.
2. معیار انتخاب: شباهت معنایی، ارتباط موضوعی و تکمیل‌کنندگی سفر کاربر.
3. برای هر لینک انتخاب شده، یک دلیل کوتاه و متقاعدکننده به فارسی بنویس.

خروجی نهایی را دقیقاً به صورت یک آرایه JSON خالص برگردان (بدون هیچ متن اضافی یا علامت \` \` \`):
[
  {
    "source_id": 1,
    "source_title": "...",
    "links": [
      { "page_id": 2, "title": "...", "reason": "..." }
    ]
  }
]

--- اطلاعات صفحات سایت ---
`;

  pages.forEach(p => {
    prompt += `ID: ${p.id} | Title: ${p.title} | Data: ${p.categories}\n`;
  });

  prompt += `\n--- کاندیداهای پیشنهادی برای هر صفحه (Top 20) ---\n`;
  
  candidatesMap.forEach((candidates, sourceId) => {
    const sourcePage = pages.find(p => p.id === sourceId);
    prompt += `Source: ${sourcePage?.title} (ID: ${sourceId})\n`;
    candidates.forEach(c => {
      prompt += `- Candidate ID: ${c.page_id} | Title: ${c.title} | Similarity Score: ${c.score} | Matched Tags: ${c.matched_tags?.join(',')}\n`;
    });
    prompt += `\n`;
  });

  return prompt;
}

export async function callGemini(prompt: string, model: string = 'gemini-3.1-flash-lite') {
  // We use our local server-side proxy to use the platform's GEMINI_API_KEY safely
  const response = await fetch('/api/gemini', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ prompt, model })
  });

  if (!response.ok) {
    const err = await response.json();
    throw new Error(err.error?.message || 'خطا در ارتباط با سرور (Gemini Proxy)');
  }

  const data = await response.json();
  let text = data.text;
  
  if (!text) throw new Error('پاسخ خالی دریافت شد');

  // Strip code blocks if AI included them or backticks
  text = text.replace(/```json/g, '').replace(/```/g, '').trim();
  
  try {
    return JSON.parse(text);
  } catch (e) {
    console.error('Failed to parse AI JSON:', text);
    throw new Error('پاسخ قابل پارس نیست. لطفاً مجدد تلاش کنید.');
  }
}
