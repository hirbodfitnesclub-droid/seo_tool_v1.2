content:# CURRENT_TASK.md — پنجره اولویت و تمرکز اجرایی > این سند بر اساس نقشه راه مرجع (`tasks.md`) ساخته شده و وضعیت تمرکز اجرایی فعلی را نشان می‌دهد. --- ## فاز جاری: رفع رگرسیون پرفورمنس در پروژه‌های کوچک/متوسط (R13) R12 (یکپارچه‌سازی ورکر + کاهش IPC) برای پروژه‌های بزرگ موفق بود، اما یک **رگرسیون اندازه‌گیری‌شده** در پروژه‌های کوچک/متوسط (~۷۰۰ صفحه) ایجاد کرد: - قبل از ریفکتور: ~۱ ثانیه روی Main Thread. - بعد از R12: چندین ثانیه به‌خاطر **سربار ثابت اسپاون ورکر + structured-clone + Vite worker bootstrap** که برای دیتای کوچک از خود محاسبه گران‌تر است. تصمیم معماری (طبق `ARCHITECTURE.md` بخش ۷.۵): **Adaptive Scoring Pipeline** — انتخاب موتور پردازشی بر اساس تعداد صفحات. --- ## تسک جاری: **R13 — Adaptive Scoring Pipeline (Fast-Track + Heavy-Track)** ### وضعیت: **🟡 در حال اجرا — منتظر پیاده‌سازی** ### هدف در `src/services/scoring/scoringService.ts` یک تابع dispatcher جدید به‌نام `computeAllAdaptive(pages)` اضافه شود که بر اساس `pages.length` بین دو موتور سوییچ می‌کند: - `pages.length <= 1000` → **Fast-Track**: اجرای همگام `computeIDFMap` و `computeAllCandidates` روی Main Thread، بدون اسپاون ورکر. - `pages.length > 1000` → **Heavy-Track**: استفاده از همان `computeAllInWorker` که در R12 ساخته شد (بدون هیچ تغییر). سپس در `src/utils/candidateStorage.ts` فقط **یک خط** تغییر کند: `computeAllInWorker(...)` با `computeAllAdaptive(...)` جایگزین شود. منطق `db.transaction` و repository‌ها دست‌نخورده می‌ماند چون قرارداد خروجی دو مسیر یکسان است. --- ## شش گام پیاده‌سازی (به ترتیب) ### گام ۱ — افزودن ثابت آستانه در `src/services/scoring/scoringService.ts` در ابتدای فایل، بعد از importها: ```ts /** آستانه تصمیم‌گیری بین مسیر همگام و مسیر ورکر — طبق ARCHITECTURE.md بخش ۷.۵ */ export const SCORING_FAST_TRACK_THRESHOLD = 1000; ``` ### گام ۲ — افزودن تابع همگام Fast-Track در همان فایل `scoringService.ts`، یک تابع داخلی **بدون export** اضافه کن که هیچ ورکری نمی‌سازد: ```ts /** * مسیر سریع (Fast-Track) — برای پروژه‌های ≤ 1000 صفحه. * مستقیماً روی Main Thread اجرا می‌شود؛ چون کل کار زیر یک ثانیه است، * هیچ نیازی به chunk-yielding یا setTimeout(0) نیست. */ function computeAllOnMainThread(pages: any[]): { idfMap: IDFMap; candidatesMap: Map<number, CandidateWithTags[]> } { // import های مورد نیاز در بالای فایل اضافه شوند: // import { computeAllCandidates } from '../../core/scoring/scorer'; // import { computeIDFMap } from '../../core/scoring/idfCalculator'; const idfMap = computeIDFMap(pages); const fullMap = computeAllCandidates(pages); // قرارداد خروجی باید با Heavy-Track یکسان باشد: حداکثر ۵۰ کاندیدا برای هر صفحه. const candidatesMap = new Map<number, CandidateWithTags[]>(); for (const [pageId, list] of fullMap.entries()) { candidatesMap.set(pageId, list.slice(0, 50)); } return { idfMap, candidatesMap }; } ``` **قواعد سخت‌گیرانه این گام:** - این تابع را به‌صورت `async` تعریف **نکن**؛ همگام بماند تا سربار microtask هم نخوریم. در dispatcher (گام ۳) آن را داخل یک تابع async پیچانده و resolve می‌کنیم. - import های `computeAllCandidates` و `computeIDFMap` در بالای فایل اضافه شوند (مستقیماً از `core/scoring/*`). - هیچ logic جدیدی فراتر از این (هیچ logging، هیچ try/catch، هیچ console) اضافه نشود. ### گام ۳ — افزودن dispatcher `computeAllAdaptive` در همان فایل، یک export جدید: ```ts /** * نقطه ورود واحد برای محاسبه IDF و کاندیداها. * بر اساس تعداد صفحات بین Fast-Track (sync, main thread) و Heavy-Track (web worker) * سوییچ می‌کند. خروجی هر دو مسیر بایت‌به‌بایت یکسان است. * * طبق ARCHITECTURE.md بخش ۷.۵. */ export async function computeAllAdaptive( pages: any[] ): Promise<{ idfMap: IDFMap; candidatesMap: Map<number, CandidateWithTags[]> }> { if (pages.length <= SCORING_FAST_TRACK_THRESHOLD) { // مسیر همگام — بدون ورکر، بدون structured-clone return computeAllOnMainThread(pages); } // مسیر سنگین — همان مسیر R12، بدون هیچ تغییری return computeAllInWorker(pages); } ``` **قواعد سخت‌گیرانه این گام:** - ⛔ تابع `computeAllInWorker` فعلی **یک کاراکتر هم تغییر نمی‌کند**. - ⛔ تابع‌های قدیمی `computeCandidatesInWorker` و `computeIDFInWorker` هم دست‌نخورده باقی می‌مانند (شاید جاهای دیگری مصرف داشته باشند). - ✅ فقط دو چیز جدید: ثابت `SCORING_FAST_TRACK_THRESHOLD`، تابع داخلی `computeAllOnMainThread`، و تابع export شده `computeAllAdaptive`. ### گام ۴ — اتصال در `candidateStorage.ts` در `src/utils/candidateStorage.ts`: - import موجود `import { computeAllInWorker } from '../services/scoring/scoringService';` را به این تبدیل کن: ```ts import { computeAllAdaptive } from '../services/scoring/scoringService'; ``` - فراخوانی `const { idfMap, candidatesMap } = await computeAllInWorker(pagesWithId);` را به این تبدیل کن: ```ts const { idfMap, candidatesMap } = await computeAllAdaptive(pagesWithId); ``` - **هیچ تغییر دیگری** در این فایل اعمال نشود. منطق `db.transaction`، تبدیل `pages` به `pagesWithId`، و ساخت `records` دقیقاً همان است. ### گام ۵ — بازبینی Pre-warm worker (مهم) در `src/main.tsx` احتمالاً خط `import('./workers/scoringWorker?worker').catch(() => {})` از R12 وجود دارد. این خط را: - اگر وجود دارد → **نگه‌اش دار**. بی‌ضرر است و فقط زمانی هزینه می‌دهد که Heavy-Track فعال شود. - اگر مزاحم Fast-Track می‌شود (مثلاً در dev موجب کندی بوت اپ می‌شود) → آن را به‌صورت idle-time-preload بپیچ: ```ts // بارگذاری ورکر فقط وقتی مرورگر بیکار است، تا Fast-Track هم سریع بوت شود. if (typeof requestIdleCallback !== 'undefined') { requestIdleCallback(() => { import('./workers/scoringWorker?worker').catch(() => {}); }); } else { setTimeout(() => { import('./workers/scoringWorker?worker').catch(() => {}); }, 2000); } ``` - اگر این خط اصلاً وجود ندارد → اضافه نکن. ### گام ۶ — سنجش دستی (Acceptance) بعد از پیاده‌سازی، روی همین دستگاه: 1. یک CSV با ~۷۰۰ ردیف import کن. زمان «آپلود → ورود به صفحه پروژه» باید **زیر ۱.۵ ثانیه** باشد، و کلیک «بررسی الگوریتم» باید **زیر ۱ ثانیه** پاسخ بدهد. 2. یک CSV با ~۵۰۰۰ ردیف import کن. زمان «بررسی الگوریتم» باید **زیر ۳ ثانیه** و UI روان باقی بماند (طبق پذیرش R12). 3. خروجی `candidates` و `idfCache` در Dexie باید بین دو حالت بایت‌به‌بایت یکسان باشد (با یک پروژه دقیقاً ۱۰۰۰ صفحه‌ای می‌توان تست diff کرد: تغییر threshold موقتاً به ۹۹۹ و سپس به ۱۰۰۱ و مقایسه خروجی). --- ## محدودیت‌های اختصاصی R13 ### ✅ بایدها - ثابت `SCORING_FAST_TRACK_THRESHOLD = 1000` دقیقاً همین مقدار باشد. - خروجی Fast-Track حتماً `slice(0, 50)` اعمال کند (پاریتی با ورکر). - تابع `computeAllAdaptive` تنها نقطه ورود جدید caller ها باشد. - منطق `db.transaction` در `candidateStorage.ts` دست‌نخورده بماند. ### ⛔ نبایدها - ⛔ هیچ تغییری در `src/core/scoring/scorer.ts`، `src/core/scoring/idfCalculator.ts`، یا `src/workers/scoringWorker.ts`. - ⛔ هیچ کتابخانه جدید نصب نشود. - ⛔ ثابت آستانه به env-var یا setting کاربری تبدیل نشود. - ⛔ ساختار خروجی `{ idfMap, candidatesMap }` تغییر نکند. - ⛔ امضای `computeAllInWorker` تغییر نکند (تابع موجود دست‌نخورده). - ⛔ Fast-Track به `async` تبدیل نشود (سربار microtask اضافی)؛ فقط dispatcher async است. --- ## آرایه کانتکست ماشین‌خوان `CONTEXT_FILES: ["Docks/ARCHITECTURE.md", "Docks/PROJECT.md", "src/services/scoring/scoringService.ts", "src/utils/candidateStorage.ts", "src/workers/scoringWorker.ts", "src/core/scoring/scorer.ts", "src/core/scoring/idfCalculator.ts", "src/main.tsx"]` > **یادآوری:** حق نداری هیچ کدی رو حدس بزنی؛ همین الان فایل‌های بالا را به‌صورت زنده فراخوانی کن و بخوان، سپس کد این تسک را بزن. --- ## رله کانتکست (Context Relay) - **چه چیزی در R11 انجام شد:** بهینه‌سازی CPU (pre-parse، slice(0,50)، CSV chunking) — صحیح بود ولی گلوگاه واقعی را هدف نگرفت. - **چه چیزی در R12 انجام شد:** حذف اسپاون متوالی دو ورکر، حذف فیلد `categories` از خروجی، یکپارچه‌سازی transaction، افزایش chunkSize، pre-warm worker، memo کردن CandidateCard. ✅ پروژه‌های بزرگ سریع شدند. - **چه چیزی R13 حل می‌کند:** رگرسیون پروژه‌های کوچک/متوسط با dispatch تطبیقی — حذف کامل سربار ورکر زمانی که اصلاً لازم نیست. - **قانون مطلق:** ضرایب، توابع، و ترتیب لایه‌های امتیازدهی scorer **یک کاراکتر هم تغییر نمی‌کند**. این تسک فقط لایه dispatcher را اضافه می‌کند. --- ## مظنونین رد شده (مهم — توضیح برای کدنویس) - ❌ **حذف کلی Web Worker و بازگشت کامل به Main Thread**: نه. برای پروژه‌های بزرگ، اجرای روی Main Thread باعث UI Freeze چند ثانیه‌ای می‌شود. Heavy-Track باید حفظ شود. - ❌ **استفاده از `requestIdleCallback` به‌جای sync در Fast-Track**: نه. کل کار زیر ۱ ثانیه است؛ idle scheduling فقط تاخیر اضافه می‌کند. - ❌ **تبدیل threshold به متغیر runtime/UI**: نه. ساده‌ترین راه‌حل = یک ثابت کد. اگر روزی نیاز شد، آن وقت پیچیده‌اش می‌کنیم. - ❌ **حذف transaction Dexie برای Fast-Track**: نه. transaction واحد در R12 ثابت شد که حتی برای دیتای کوچک هم سود می‌دهد (notification واحد به‌جای چندتایی).